package maekawa;

/**
 * Enum that contains each one of the states that a node can have.
 *
 * @author Cassio dos Santos Sousa <dssntss2@illinois.edu>
 * @version 1.0
 */
public enum State {
    INIT, REQUEST, HELD, RELEASE
}
